# Evaluation configs

The reproducible paper evaluation is currently configured directly inside `evaluation/paper_validation.py` so that scenario definitions, controller variants, and output table generation remain in one auditable entrypoint.

This directory is reserved for future external scenario/config files if the evaluation suite grows beyond the current paper setup.
