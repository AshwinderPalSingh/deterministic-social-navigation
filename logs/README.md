# Logs

This repository keeps the full reproducible paper logs under `results/paper_eval/`.

The `logs/` directory is reserved for optional small sample logs or ad hoc local debugging and is intentionally kept minimal in the tracked repository.
